# UTV2-1736 forward partition provisioning
# Placeholder created at lane admission; implemented in-lane.
name: Partition Provisioning
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo placeholder
