# Break-Glass Dry Run — DRYRUN-20260710-01

> **THIS IS A DRY RUN. NO REAL INCIDENT OCCURRED. NO PRODUCTION SYSTEM WAS PAUSED, ROLLED BACK, OR RESTORED.**
> This artifact exercises the mechanics of `docs/05_operations/BREAK_GLASS_PROTOCOL.md` end-to-end using a
> hypothetical scenario, per the OS v1 stabilization wave (UTV2-1520). It intentionally uses the `DRYRUN-`
> filename prefix (not `INC-`) so it is never mistaken for a real incident declaration by `pnpm ops:digest`'s
> overdue-break-glass-review check or any other tooling that scans `docs/06_status/incidents/INC-*.md`.
> The Section 5 mandatory 48-hour PM review window does **not** apply to this artifact, because no real
> break-glass action was taken.

## What this validates

Every mechanical step in `BREAK_GLASS_PROTOCOL.md` Sections 1, 3, 4, and 6, walked through against a
plausible-but-fictional scenario, to confirm the procedure is actually executable by the current sole
invoker (Claude, per Section 2, Option A) rather than only theoretically defined.

## Hypothetical scenario (fictional — for dry-run purposes only)

Assume: the candidate-pick-scanner begins writing picks with `stake_units = null` at a sustained rate,
and those picks are visibly reaching the member-facing delivery path within the hour (mirroring the
real historical class of incident recorded as `DEBT-002`, but not an actual recurrence — DEBT-002 was
closed 2026-05-11 and this scenario is invented for this exercise only).

## Section 1 — When break-glass may be used (all four conditions walked through)

1. **Active production emergency** — *(hypothetical)* Yes: malformed picks reaching members is a live
   data-integrity/customer-harm condition per the protocol's own examples.
2. **Only a permitted action resolves it** — *(hypothetical)* Yes: the correct response is to **pause**
   the candidate-pick-scanner (Section 4, second bullet: "pause a running process... promotion path"),
   not to ship a fix. A forward-fix for the null-`stake_units` bug would be a normal T1/T2 lane, not
   break-glass — break-glass only stops the bleeding.
3. **Griff unreachable within the response window** — *(hypothetical, simulated for this exercise only)*
   Griff is in fact reachable in the live session that produced this dry run; the two-attempt,
   two-channel, 30-minute evidence below is a **simulated** example of what the evidence section would
   contain, not a real unreachability claim:
   - Simulated attempt 1: Discord DM, 2026-07-10T18:00:00Z, no response.
   - Simulated attempt 2: phone call, 2026-07-10T18:35:00Z, no response (35 minutes after attempt 1,
     satisfying the ">= 30 minutes across >= 2 channels" requirement).
4. **Waiting would increase harm beyond what the action prevents** — *(hypothetical)* Yes: every additional
   minute the scanner runs writes more malformed picks that would need individual correction.

All four conditions are satisfiable in a real incident's evidence section without needing a fifth
invented condition — confirms Section 1's structure is usable as written.

## Section 3 — Incident declaration artifact (filled per the required template)

```
# Incident Declaration (SIMULATED — see DRYRUN banner above)

Incident ID: DRYRUN-20260710-01
Declared at: 2026-07-10T18:35:00Z
Declared by: Claude (orchestrator, current session)

## What is happening
(Hypothetical) candidate-pick-scanner is writing picks with stake_units=null at a sustained rate; a
sample of these have reached the member-facing delivery path within the last hour. Evidence would
normally cite: apps/api/src/ logs showing the null-write pattern, and a delivery-outbox query showing
affected pick IDs already sent.

## Why this qualifies for break-glass
Maps to Section 1 conditions 1-4 above: live data-integrity harm reaching members (1); pausing the
scanner is a Section 4 permitted action and a forward-fix is explicitly out of scope for break-glass (2);
two documented unreachability attempts, two channels, 35 minutes apart (3); each additional minute
increases the volume of malformed picks needing correction (4).

## Griff unreachability evidence
- Attempt 1: Discord DM, 2026-07-10T18:00:00Z, no response.
- Attempt 2: phone call, 2026-07-10T18:35:00Z, no response.
(SIMULATED for this dry run — Griff was in fact reachable in the live session.)

## Proposed permitted action
Pause: disable SYSTEM_PICK_SCANNER_ENABLED (the same flag PM used to re-enable the scanner per
UTV2-587/DEBT-003), restoring the last-known-good state of "scanner off," which was itself a prior
PM-approved configuration before the 2026-04-24 re-enable. This is a Section 4 "pause a running
process... promotion path" action, not a new capability.

## Rollback-of-the-rollback plan
Re-enable SYSTEM_PICK_SCANNER_ENABLED=true once Griff's post-hoc review (Section 5) rules on the
incident and, if applicable, once the underlying null-stake_units write path has a normal-lane fix
merged and verified.
```

**Result: the template is fully fillable with concrete, specific values — no placeholder text was needed
beyond the two fields legitimately marked SIMULATED in this exercise.**

## Section 4 — Permitted action selection

Selected: **Pause** (`docs/05_operations/BREAK_GLASS_PROTOCOL.md` Section 4, bullet 2). Confirmed this
scenario does **not** fall into any of the explicitly-forbidden categories (no new code, no migration, no
delivery/outbox expansion, no pricing/customer-facing activation, no `merge-gate.yml`/CODEOWNERS/branch
protection change, no deploy-authority expansion) — a config-flag toggle to *stop* an existing process is
squarely a pause action.

**No action was actually taken.** `SYSTEM_PICK_SCANNER_ENABLED` was not modified; the scanner was not
touched; no commit or deploy occurred as part of this dry run.

## Section 6 — Fail-closed rules checked

- [x] Incident declaration artifact produced (this file) — the "no artifact, no action" gate is satisfiable.
- [x] Two unreachability attempts, two channels, 30+ minutes apart — the evidence shape is satisfiable
      (simulated values used here; a real invocation would need genuine timestamps/channels).
- [x] Proposed action maps cleanly to exactly one of the three Section 4 permitted actions (pause) — no
      ambiguity between rollback/pause/restore in this scenario.
- [x] A concrete "safe last state" was nameable (`SYSTEM_PICK_SCANNER_ENABLED=false`, the pre-2026-04-24
      configuration) — confirms the "if you cannot name the exact prior state, you cannot invoke" rule
      is checkable, not just aspirational.
- [x] Section 8's open invoker-model decision (Option A/B/C) does not block this dry run: Option A
      (Claude-only invoker, current default) is what this exercise validates; Options B/C remain a
      PM decision, unaffected by this run.

No fail-closed condition was tripped in a way that would have blocked a genuine invocation of this
hypothetical scenario under Option A.

## Section 5 — Post-hoc review (explicitly not triggered)

This dry run does **not** start the Section 5 48-hour PM review clock, because Section 5 only applies to
an actual break-glass invocation. Nothing here requires Griff's review under that section. This section
is included only to confirm the reader that no such review is pending as a result of this exercise.

## Section 7 — Lane and truth-model mechanics observed

This dry run is itself tracked as exactly one issue (UTV2-1520) → one lane → one branch → one PR,
consistent with CLAUDE.md invariant 3, even though the artifact's *subject matter* is a hypothetical
incident rather than new product code — confirming the lane apparatus and the break-glass apparatus
compose without conflict.

## Outcome

The break-glass procedure in `BREAK_GLASS_PROTOCOL.md` is mechanically executable today under the
Option A (Claude-only) invoker model: the incident declaration template can be filled with real,
specific content; the permitted-action categories are distinguishable in practice; the fail-closed
gates are checkable rather than purely aspirational. No gaps were found that would block a genuine
invocation. Section 8's invoker-model decision (A/B/C) remains open and is unaffected by this result.
