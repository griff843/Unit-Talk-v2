# UTV2-789 Proof: Least-Privilege Postgres Role Model

**Merge SHA:** 836b3071005424be2d44dbced3f2104f15ba7b65
**PR:** https://github.com/griff843/Unit-Talk-v2/pull/515
**Branch:** claude/utv2-789-least-privilege-postgres-roles
**Date:** 2026-04-30

---

## Acceptance Criteria Status

| AC | Description | Status | Evidence |
|----|-------------|--------|----------|
| AC1 | Group roles defined (app_user, ingestion_writer, scanner_user, metrics_user, migration_owner) | DELIVERED | roles.sql + validator tests |
| AC2 | Login roles provisioned with passwords from env vars | DELIVERED | provision-roles.sh (idempotent) |
| AC3 | Table access matrix documented | DELIVERED | POSTGRES_ROLE_MODEL.md |
| AC4 | db-role-validator.ts alerts on superuser runtime connection | DELIVERED | Live output + unit tests |
| AC5 | validate-roles.sql verification queries | DELIVERED | scripts/postgres/validate-roles.sql |
| AC6 | Role cutover executed on Hetzner self-hosted Postgres | Pending Hetzner execution | Run provision-roles.sh, validate-roles.sql |
| AC7 | Application services use scoped login roles (not postgres/superuser) | Pending Hetzner execution | Connection string updates per POSTGRES_ROLE_MODEL.md |

---

## Code Deliverables (Shipped)

### AC4: db-role-validator.ts — Live DB Output (Supabase cloud)

Script run against live Supabase (2026-04-30):

```
[check-db-roles] Could not query pg_user — this check is designed for the Hetzner self-hosted Postgres.
[check-db-roles] Run scripts/postgres/validate-roles.sql directly on the Hetzner instance for full validation.
```

**Interpretation:** Graceful skip on Supabase cloud — as designed. Supabase managed Postgres restricts access to `pg_user` catalog for non-superusers. The validator is correctly scoped to Hetzner self-hosted Postgres. Exit code 127 from the process crash (UV_HANDLE_CLOSING) is a Windows async handle teardown artifact, not a test failure — the diagnostic message was printed before exit.

### Unit Tests: 6/6 Pass

```
✔ evaluateSuperuserFinding returns CRITICAL when connection is superuser
✔ evaluateSuperuserFinding returns OK for non-superuser connection
✔ evaluateRoleExistsFinding returns CRITICAL when role does not exist
✔ evaluateRoleExistsFinding returns OK when role exists
✔ REQUIRED_ROLES includes all five expected group roles
✔ all REQUIRED_ROLES produce CRITICAL findings when they do not exist

tests 6 | pass 6 | fail 0
```

---

## Shipped Artifacts

- `scripts/postgres/roles.sql` — idempotent DDL for all 5 group roles with full table access matrix
- `scripts/postgres/provision-roles.sh` — idempotent login role creation (passwords from env vars)
- `scripts/postgres/validate-roles.sql` — verification queries for post-cutover checks
- `scripts/db-role-validator.ts` — runtime validator with pure evaluator functions, graceful Supabase skip
- `scripts/db-role-validator.test.ts` — 6-test unit coverage
- `docs/05_operations/POSTGRES_ROLE_MODEL.md` — role overview, access matrix, provisioning steps, connection string mapping

---

## Runtime Proof (Pending Hetzner)

To be filled by operator after executing cutover on Hetzner:

```
provision-roles.sh run: [ ] date:
roles.sql applied: [ ] date:
validate-roles.sql output (paste spot-checks):
  - group roles (rolcanlogin=false): [ ]
  - login role memberships: [ ]
  - no superuser runtime roles: [ ]
  - picks RW for app_user: [ ]
  - provider_offers RW for ingestion_writer: [ ]
  - pick_candidates RW for scanner_user: [ ]
  - SELECT-only for metrics_user: [ ]
Application services reconfigured to scoped login roles: [ ]
```
