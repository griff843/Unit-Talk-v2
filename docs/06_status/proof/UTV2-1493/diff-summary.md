## Summary

UTV2-1493 defines the Break-Glass Merge & PM Continuity Protocol: a documented, evidence-gated, time-boxed, mandatorily-reviewed emergency procedure for when Griff (the sole PM approval authority per `.github/CODEOWNERS` and `docs/05_operations/schemas/pm-verdict-v1.md`) is unreachable. This is a governance/docs-only lane per PM-approved scope — no code, no `merge-gate.yml` change, no `CODEOWNERS` change, no branch protection change.

PM-approved principle (2026-07-08): emergency action may restore the last PM-approved safe state; emergency action may not create new product/runtime authority. This principle is encoded directly into the protocol's scope (Section 4 — rollback/pause/restore only, explicit forbidden-actions list matching PM's hard constraints).

## Evidence

Branch: `claude/utv2-1493-break-glass-merge-pm-continuity`
Head SHA (pre-merge): `b2418fd75ba6f00fb5feae2b8401cf26f1f2c205`

Changed files (scope matches the declared `file_scope_lock`):

- `docs/05_operations/BREAK_GLASS_PROTOCOL.md` (new) — the protocol itself. Addresses every acceptance criterion from the issue by section: when break-glass may be used (§1), who may invoke it (§2), required evidence (§3), permitted emergency-only actions and merge mechanics (§4/§4a), mandatory post-hoc PM review within a fixed 48h window (§5), fail-closed rules (§6), preserved lane/truth-model invariants (§7), an explicit PM decision section presenting three invoker models rather than unilaterally picking one (§8), and explicit non-goals restating the issue's Do-Not clause (§9).
- `docs/06_status/lanes/UTV2-1493.json` and `.ops/sync/UTV2-1493.yml` — lane manifest and sync metadata, auto-generated by `ops:lane-start` in an earlier commit on this branch (`3df2090b`), unmodified by this commit.

**Not touched:** `.github/workflows/merge-gate.yml`, `.github/CODEOWNERS`, any branch protection setting, any `packages/**`/`apps/**` runtime or product code, `docs/05_operations/schemas/pm-verdict-v1.md` (existing schema untouched — break-glass does not introduce a competing verdict authority), `docs/05_operations/DIRECT_MAIN_BYPASS_POLICY.md` (referenced, not modified).

## Verification

Verification is recorded in `docs/06_status/proof/UTV2-1493/verification.md`.

## Merge SHA binding

Head SHA: b2418fd75ba6f00fb5feae2b8401cf26f1f2c205
Merge SHA: (populated post-merge by `ops:proof-generate --merge-sha`)
