# Diff Summary — UTV2-1104

Three files changed to implement the `GovernanceException` runtime entity. `packages/invariants/src/governance-exception.ts` was created as a new module containing the `GovernanceExceptionInput`, `GovernanceException`, and `ExceptionAuthorization` interfaces, the `GovernanceExceptionType` union, the `GovernanceExceptionValidationError` class, and the `createGovernanceException` factory function with full field validation and `AuditEvent` emission — all pure TypeScript with no I/O. `packages/invariants/src/governance-exception.test.ts` was created with 15 adversarial `node:test` test cases covering happy-path creation, auditEvent field correctness, all individual validation failure paths (missing/empty/invalid fields, self-approval, past expiration, short justification), and JSON round-trip replayability. `packages/invariants/src/index.ts` was updated to re-export `createGovernanceException`, `GovernanceExceptionValidationError`, and all associated types so consumers can import them from `@unit-talk/invariants`.
