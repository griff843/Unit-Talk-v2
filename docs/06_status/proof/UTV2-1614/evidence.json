{
  "schema_version": 1,
  "issue_id": "UTV2-1614",
  "sha_binding": {
    "verified_source_sha": "529b26f552b74113fb8b5ad9ce6742613e7c239a",
    "merge_sha": null,
    "evidence_commit_sha": "529b26f552b74113fb8b5ad9ce6742613e7c239a",
    "current_pr_head_sha": "529b26f552b74113fb8b5ad9ce6742613e7c239a",
    "verified_source_note": "Bound to 529b26f552b74113fb8b5ad9ce6742613e7c239a, the commit carrying the final state of every in-scope file. Revision history: da298435 was rejected by exact-head review; 60c9598a closed those ten items; e829d2bb repaired two contract tests that depended on git objects unavailable on a fetch-depth-1 CI checkout; 408454a4 closed the first two adversarial reviews' findings; 4e46becb closed the third review's; 529b26f552b74113fb8b5ad9ce6742613e7c239a closes the fourth review's. No existing refusal was relaxed except one that was WRONG: rejecting the repository's own CI-resolved sentinels, which 25 real bundles legitimately store."
  },
  "verifier": {
    "identity": "claude-opus-5",
    "role": "orchestrator"
  },
  "tier": "T1",
  "static_proof": {
    "pnpm_verify": {
      "status": "PASS",
      "note": "type-check, lint and test:ops clean on 529b26f552b74113fb8b5ad9ce6742613e7c239a, run sequentially; verify:parallel OOM-killed locally (exit 137), not a waiver."
    },
    "type_check": {
      "status": "PASS"
    },
    "lint": {
      "status": "PASS"
    },
    "build": {
      "status": "PASS"
    },
    "test": {
      "status": "PASS",
      "note": "test:ops 1329 passing, including 66 proof-rebind assertions (was 1299 / 36 at the first rejected head)."
    },
    "test_run_logs": [
      {
        "path": "scripts/ops/proof-rebind.test.ts",
        "merge_sha": null
      }
    ],
    "mutation_verification": {
      "status": "PASS",
      "method": "spot-verified",
      "note": "Every fix across this revision chain was reverted individually on scratch copies and the suite re-run; every revert fails. Twenty-seven verified in total. This revision adds five: setext heading recognition removed, HTML comment verbatim handling removed, inline-code stripping removed, owner total-length cap reverted, and set-by-ci removed from the placeholder set. Three mutations the reviews performed themselves — the mask comparison, the changed-line planner call, and the CLI apply-without-validation gate — previously left the suite green and now each fail the specific test that claims to cover them."
    },
    "r_level_check": {
      "status": "PASS",
      "verdict": "PASS",
      "changed_files": 7,
      "rules_matched": "ops tooling + its test + test wiring only; no application, domain, package or migration path touched"
    },
    "regression_sweep": {
      "status": "PASS",
      "scope": "all 591 artifacts under docs/06_status/proof — 397 verification.md and 194 evidence.json",
      "method": "each artifact re-planned under the previous ruleset and the new one, each verification.md using its OWN PR row so PR identity is never the differentiator, and the per-artifact refusal sets compared",
      "result": "identical 574; newly planned 6 (5 bundles carrying set-by-ci, plus this lane's own bundle unblocked by the inline-<pre> fix); same verdict with the set-by-ci refusal class removed 11; NEWLY REFUSED 0",
      "correction": "the previous head claimed 397/397 unchanged. That covered only verification.md — evidence.json, where the sentinel values live, was never swept, which is how the set-by-ci over-strictness went unnoticed. The claim also stopped being true for that head once its own proof bundle documented the <pre> rule and was newly refused by its own tool."
    }
  },
  "runtime_proof": {
    "command": "pnpm test:db",
    "status": "PASS",
    "tests": 7,
    "pass": 7,
    "fail": 0,
    "duration_ms": 94812,
    "queries": [
      "database repository bundle persists a submission and settlement against live Supabase",
      "atomic enqueue rollback verification",
      "atomic delivery confirmation rollback",
      "atomic settlement rollback",
      "no-duplicate-participants check",
      "re-settlement correction chain integrity"
    ],
    "receipts": [
      {
        "supabase_project": "zfzdnfwdarxucxtaojxm",
        "duration_ms": 94812,
        "tests_run": 7,
        "tests_pass": 7,
        "note": "Live Supabase. Ops tooling lane with no DB schema or query changes; test:db establishes the live-DB suite is undisturbed. 7/7 clean on this head. Earlier on this branch one run failed 1 of 7 with 'Failed to list participants by type: TypeError: fetch failed', a transport-level failure reaching Supabase; no in-scope file touches the DB layer and every subsequent run passed 7/7. That failing run is recorded, not claimed as a pass. Fixture rows written by the suite are test artifacts and must be excluded from production counts."
      }
    ],
    "additional": "ops:proof-rebind preview run against two real bundles; --apply was never used and git status reported both proof directories clean afterwards. The PR-1311 lane bundle returns proof_rebind_refused (canonical '## Merge SHA Binding' section absent; sha256_before == sha256_after for both artifacts). The PR-1313 lane bundle returns proof_rebind_preview with errors: [] and exactly five binding changes, with the line count unchanged, only the declared lines differing, the verified_source_note narrative byte-identical, and the out-of-section narrative row 'Merge SHA: pending merge. This bundle is bound to ...' untouched. The previous revision's proof described the PR-1313 bundle as refused for carrying explanatory prose; that statement was wrong and is retracted. Both contracts are pinned as tests that read the bundles from commit 2822b709c74c43dc24a50dc6df35597e1a0463fe via git show, so they cannot change meaning if either bundle is later legitimately rebound."
  },
  "blast_radius": {
    "production_mutation_on_merge": false,
    "note": "Merging adds a CLI that defaults to preview and cannot write without a completed PR identity validation."
  }
}
