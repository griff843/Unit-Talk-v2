{
  "schema_version": 1,
  "issue_id": "UTV2-1589",
  "title": "Bind model-routing provenance to the authoritative merge SHA",
  "tier": "T1",
  "status": "ready_for_pm_review",
  "generated_at": "2026-07-25T16:00:00Z",
  "sha_binding": {
    "verified_source_sha": "679b8af4ff1b048230cf1cb3c295d484b2a32014",
    "verified_source_note": "Final substantive implementation commit. Includes: the PM-authorized first scope expansion binding model-routing.json during the trusted scripts/ops/lane-close.ts --repair-merged path (rebindRepairedLaneProof now validates and binds every declared model-routing.json sidecar before writing any proof artifact, propagating malformed/missing/identity-mismatch/conflicting-binding failures as a new model_routing_rebind_failed closeout code); a fresh-context independent review's hardening finding (rebindRepairedLaneProof reuses proof-generate.ts's safeRepoPath repo-root escape guard instead of a bare path.resolve); a PM-authorized second scope expansion fixing a workflow/model-routing deadlock (post-merge-lane-close.yml's 'Bind proof artifacts to merge SHA' step ran on workflow_dispatch too, binding model-routing.json to main's current tip -- not the historical lane's authoritative merge SHA -- before --repair-merged could bind the real one, permanently deadlocking the replay via binding_conflict; restricted that step to push events only), independently re-reviewed with no blocking defect; a fourth Codex review finding fixed (rebindModelRoutingJsonSha required only a matching issue_id, so a truncated/tampered sidecar like {\"issue_id\":\"UTV2-1586\"} would receive an authoritative closeout_binding despite carrying no real execution-provenance fields -- now requires non-empty model and reasoning_effort string fields on any required sidecar); plus the two prior independent-review-driven fixes to scripts/ops/proof-generate.ts: (1) the CLI reports a structured nonzero failure instead of an uncaught crash on a model-routing binding conflict; (2) rebindModelRoutingJsonSha rejects an identity-less or wrong-lane sidecar (issue_id mismatch, including {}) instead of silently binding it. Post-merge proof generation rebinds this field to the authoritative GitHub merge SHA.",
    "evidence_commit_sha": "set-by-ci",
    "current_pr_head_sha": "set-by-ci",
    "sha_type": "substantive_commit_sha",
    "bound_at": "2026-07-25T16:00:00Z",
    "pr_number": null,
    "approved_head_sha": null
  },
  "verifier": {
    "identity": "codex/utv2-1589-model-routing-sha-bind",
    "role": "implementation-lane",
    "independence_note": "Executor-produced mechanical evidence; this is not PM approval or independent certification."
  },
  "scope": {
    "implementation_paths": [
      ".github/workflows/post-merge-lane-close.yml",
      "scripts/ops/lane-close.ts",
      "scripts/ops/lane-close.test.ts",
      "scripts/ops/proof-generate.ts",
      "scripts/ops/proof-generate.test.ts",
      "scripts/ops/truth-check-lib.test.ts"
    ],
    "proof_paths": [
      "docs/06_status/proof/UTV2-1589/evidence.json",
      "docs/06_status/proof/UTV2-1589/model-routing.json",
      "docs/06_status/proof/UTV2-1589/verification.md"
    ],
    "runtime_change": false,
    "workflow_change": true,
    "workflow_change_note": "PM-authorized second scope amendment: one-line `if:` condition added to post-merge-lane-close.yml's 'Bind proof artifacts to merge SHA' step (restricts it to push events). No new triggers, permissions, secrets, or jobs added; existing trusted-context validation (isTrustedPostMergeAutomation) and Merge Gate/branch protection are unchanged.",
    "database_change": false,
    "branch_protection_change": false,
    "approval_policy_change": false
  },
  "static_proof": {
    "pnpm_verify": {
      "command": "pnpm verify",
      "status": "PASS",
      "exit_code": 0,
      "detail": "Environment validation, lint, project-reference type checking, build, aggregate tests, live database smoke tests, and the T1 live-proof battery passed."
    },
    "focused_proof_tests": {
      "command": "npx tsx --test scripts/ops/proof-generate.test.ts scripts/ops/truth-check-lib.test.ts scripts/ops/lane-close.test.ts",
      "status": "PASS",
      "tests": 217,
      "pass": 217,
      "fail": 0,
      "skipped": 0,
      "detail": "Covers provenance preservation, idempotency, PR/SHA conflicts, malformed and missing sidecars, optional sidecars, atomic preflight, the UTV2-1585/#1305 and UTV2-1586/#1306 fixtures, and P3/C4 before-and-after behavior in scripts/ops/proof-generate.test.ts and scripts/ops/truth-check-lib.test.ts. scripts/ops/lane-close.test.ts additionally proves the trusted --repair-merged path itself (rebindRepairedLaneProof) binds every declared model-routing.json sidecar -- not just evidence.json/verification.md -- against the real UTV2-1585/#1305 and UTV2-1586/#1306 identities, is idempotent on replay, fails closed (with zero proof mutation) on a conflicting PR, conflicting SHA, invalid JSON, wrong/missing issue identity, and a missing required sidecar, leaves a lane with no declared sidecar unaffected, satisfies the C4 closeout gate and P3's merge-SHA-reference predicate through that same real path, and that a failed rebind rolls back the manifest and every proof file (including model-routing.json) through the existing repair rollback transaction."
    },
    "type_check": {
      "command": "pnpm type-check",
      "status": "PASS"
    },
    "root_test": {
      "command": "pnpm test",
      "status": "PASS"
    },
    "manifest_validation": {
      "command": "npx tsx scripts/ops/lane-manifest.ts validate UTV2-1589 --json",
      "status": "PASS",
      "code": "manifest_valid"
    },
    "diff_check": {
      "command": "git diff --check",
      "status": "PASS"
    },
    "r_level": {
      "command": "npx tsx scripts/ci/r-level-check.ts --base origin/main --head HEAD",
      "status": "PASS",
      "rules_matched": []
    }
  },
  "runtime_proof": {
    "command": "pnpm test:db",
    "status": "PASS",
    "pnpm_test_db": "pass",
    "tests": 7,
    "pass": 7,
    "fail": 0,
    "supabase_project": "zfzdnfwdarxucxtaojxm",
    "target_supabase_project": "zfzdnfwdarxucxtaojxm",
    "classification": "Operations proof-tooling change; the mandatory T1 live database and runtime-proof gates passed without lane-authored database mutation.",
    "production_mutation_by_lane": false,
    "queries": [
      {
        "table": "picks",
        "description": "The live database smoke suite persisted and settled a unique submission through the repository abstraction, then cleaned up its fixture."
      },
      {
        "table": "pick_lifecycle",
        "description": "The live database smoke suite verified invalid atomic operations leave no partial lifecycle writes."
      },
      {
        "table": "distribution_outbox",
        "description": "The live database smoke suite verified atomic claim and invalid delivery paths do not double-claim or leave partial outbox mutation."
      },
      {
        "table": "audit_log",
        "description": "The live suites exercised additive audit behavior and rollback assertions through existing test paths."
      },
      {
        "table": "settlement_records",
        "description": "The live database smoke suite verified corrections remain additive and do not mutate the original settlement."
      }
    ],
    "row_counts": [
      {
        "table": "picks",
        "count": 196,
        "status": "preexisting_awaiting_approval_rows_observed"
      }
    ],
    "row_counts_captured_at": "2026-07-25T04:58:58Z",
    "row_counts_note": "The existing T1 proof battery reported 196 pre-existing stranded awaiting_approval rows. This lane did not update, delete, or backfill them.",
    "broader_live_proof": {
      "command": "pnpm test:t1-proof:live",
      "status": "PASS",
      "fail": 0,
      "skipped": 1,
      "skip_reason": "The latest provider snapshot was older than the bounded lookback window; the test classifies this as stale provider data rather than a code failure."
    }
  },
  "acceptance_criteria_mapping": [
    {
      "criterion": "Post-merge proof generation appends an authoritative closeout binding without replacing execution provenance.",
      "status": "PASS",
      "evidence": "rebindModelRoutingJsonSha preserves every parsed top-level field, including unknown fields, and adds only closeout_binding with sha_type, merge_sha, pr_url, and bound_at."
    },
    {
      "criterion": "Rebinding is idempotent for the same PR/SHA and fails closed for a different PR or SHA.",
      "status": "PASS",
      "evidence": "Focused tests prove exact reruns preserve the original bound_at and conflicting PR or merge SHA values throw binding_conflict before any proof write."
    },
    {
      "criterion": "Required missing or malformed model-routing sidecars fail, while optional missing sidecars remain unaffected.",
      "status": "PASS",
      "evidence": "Focused tests cover required missing, invalid JSON, malformed object, missing PR URL, and optional missing cases with distinct fail-closed outcomes."
    },
    {
      "criterion": "Existing evidence and verification rebinding behavior remains unchanged.",
      "status": "PASS",
      "evidence": "The specialized model-routing preflight is composed with the existing evidence.json and verification Markdown rebinders; the aggregate proof suite passes."
    },
    {
      "criterion": "P3 and C4 fail before closeout binding and pass after authoritative binding.",
      "status": "PASS",
      "evidence": "truth-check-lib focused tests exercise the same sidecar before and after closeout_binding and retain the existing P3/C4 interpretation."
    },
    {
      "criterion": "Historical repair fixtures use the real UTV2-1585 and UTV2-1586 merge identities.",
      "status": "PASS",
      "evidence": "Focused tests use UTV2-1585 PR #1305 at 97527b791fc37acce41f4f46fd88699dce054b66 and UTV2-1586 PR #1306 at fe09f637a7eeebf216e062dd4a003d7e38932d1a."
    },
    {
      "criterion": "PM scope amendment: the trusted scripts/ops/lane-close.ts --repair-merged path binds every required model-routing.json sidecar before truth-check, not just evidence.json/verification.md.",
      "status": "PASS",
      "evidence": "rebindRepairedLaneProof() now validates (write:false) every model-routing.json path declared in expected_proof_paths before writing any proof artifact, then binds it alongside the existing evidence/verification rebind. scripts/ops/lane-close.test.ts proves this against the real UTV2-1585/#1305 and UTV2-1586/#1306 identities, idempotent replay, binding_conflict on a different PR or SHA, malformed_required_sidecar on invalid JSON, sidecar_identity_mismatch on wrong/missing issue_id, missing_required_sidecar when the declared file is absent, no effect on a lane with no declared sidecar, C4/P3 passing through the real path, and rollback of the manifest and every proof file (via the existing createRepairRollbackTransaction, which already snapshots the whole proof directory) when the rebind fails."
    },
    {
      "criterion": "A model-routing rebind failure during trusted repair fails closed with a distinct, propagated error rather than a generic infra_error or silent partial bind.",
      "status": "PASS",
      "evidence": "main()'s catch block now special-cases ModelRoutingRebindError (checked before the generic fallback) and emits a model_routing_rebind_failed closeout code carrying the underlying model_routing_error_code, proof_path, and message, with transaction.rollback() already invoked unconditionally at the top of that catch block."
    },
    {
      "criterion": "The trusted workflow_dispatch replay path for UTV2-1585/UTV2-1586 does not deadlock model-routing.json's immutable closeout_binding against a non-authoritative SHA before --repair-merged can bind the real one.",
      "status": "PASS",
      "evidence": "post-merge-lane-close.yml's 'Bind proof artifacts to merge SHA' step is now restricted to push events (github.event_name == 'push'); a workflow-assertion test in scripts/ops/lane-close.test.ts confirms this condition is present."
    },
    {
      "criterion": "A required model-routing sidecar cannot receive an authoritative closeout_binding unless it carries real execution-provenance fields, not merely a matching issue_id.",
      "status": "PASS",
      "evidence": "rebindModelRoutingJsonSha now requires non-empty model and reasoning_effort string fields on any required sidecar (checked after identity validation, before the existing conflict check). Three new focused tests cover a truncated {issue_id}-only sidecar, a sidecar with reasoning_effort present but blank, and confirm optional (non-required) sidecars remain unaffected."
    },
    {
      "criterion": "A governed manifest-only repair PR merging via an ordinary push (not just a workflow_dispatch replay) does not re-trigger the model-routing deadlock.",
      "status": "PASS",
      "evidence": "The 'Bind proof artifacts to merge SHA' step now additionally reads the checked-out manifest's own commit_sha and skips the early proof-generate bind whenever it already disagrees with this push's SHA -- the signal that this push is a repair commit (e.g. from buildRepairRequiredViaPrPacket's recommended path), not the lane's own first-time implementation merge. A workflow-assertion test confirms the guard is present and that the pnpm ops:proof-generate call is gated behind it, not run unconditionally alongside it."
    }
  ],
  "historical_repair_inventory": [
    {
      "issue_id": "UTV2-1585",
      "manifest_status": "started",
      "pr_url": "https://github.com/griff843/Unit-Talk-v2/pull/1305",
      "merge_sha": "97527b791fc37acce41f4f46fd88699dce054b66",
      "sidecar_state": "present_without_closeout_binding",
      "recovery": "Replay trusted post-merge closeout after UTV2-1589 merges, supplying PR #1305 explicitly."
    },
    {
      "issue_id": "UTV2-1586",
      "manifest_status": "in_review",
      "pr_url": "https://github.com/griff843/Unit-Talk-v2/pull/1306",
      "merge_sha": "fe09f637a7eeebf216e062dd4a003d7e38932d1a",
      "sidecar_state": "present_without_closeout_binding",
      "recovery": "Replay trusted post-merge closeout after UTV2-1589 merges; the recorded PR identity is sufficient."
    },
    {
      "issue_id": "UTV2-1589",
      "manifest_status": "started",
      "pr_url": null,
      "merge_sha": null,
      "sidecar_state": "premerge_provenance_present",
      "recovery": "Close normally through the repaired post-merge path after this implementation PR merges."
    }
  ],
  "known_limitations": [
    "Historical replay of UTV2-1585 and UTV2-1586 is a post-merge follow-up and is not performed by this implementation PR.",
    "The T1 approval label and PM verdict remain independent external merge requirements.",
    "RESOLVED (was a KNOWN GAP): scripts/ops/lane-close.ts's rebindRepairedLaneProof() previously called only rebindMergeSha(), never invoking this issue's model-routing rebind, so a trusted --repair-merged replay of UTV2-1585/UTV2-1586 would still have failed P3/C4 on model-routing.json even after this PR merged. Found by independent Codex review; fixed under a PM-authorized same-lane scope expansion adding scripts/ops/lane-close.ts and scripts/ops/lane-close.test.ts to this issue's file scope. rebindRepairedLaneProof() now validates and binds every declared model-routing.json sidecar before writing any proof artifact, propagating malformed/missing/identity-mismatch/conflicting-binding failures as a new model_routing_rebind_failed closeout code before truth-check runs.",
    "RESOLVED: a fresh independent review of the trusted-repair fix found rebindRepairedLaneProof() resolved manifest-declared model-routing.json paths via a bare path.resolve() with no repo-root escape check, unlike generateProofArtifacts()'s safeRepoPath() guard for the same kind of manifest-declared path. Fixed by exporting and reusing safeRepoPath in lane-close.ts, with a regression test proving an escaping path (e.g. ../../../../tmp/...) is refused.",
    "ACCEPTED, not fixed: the same independent review also noted that generateProofArtifacts() (proof-generate.ts) binds only the first model-routing.json match via .find() when a manifest declares more than one such sidecar, while rebindRepairedLaneProof() (this PR's fix) binds every match via .filter(). No lane in practice declares more than one model-routing.json sidecar (lane-start.ts always writes exactly one canonical path per issue), so this is a theoretical divergence, not an active defect; changing generateProofArtifacts()'s existing, already-shipped .find() behavior was judged out of this issue's authorized scope (PM explicitly limited this expansion to the lane-close.ts repair path) and is left for a follow-up hardening pass if the single-sidecar-per-lane invariant is ever relaxed.",
    "ACCEPTED, not fixed: createRepairRollbackTransaction() snapshots the whole docs/06_status/proof/<issueId>/ directory and relies on every bound model-routing.json sidecar living inside it -- true under the sanctioned lane lifecycle (lane-start.ts always writes proof paths under that directory) but not independently enforced by rebindRepairedLaneProof() or the rollback transaction itself. Combined with the now-fixed path-escape guard, an out-of-directory-but-still-in-repo sidecar path remains a theoretical gap gated behind an already-abnormal, non-lane-start-produced manifest; not fixed here to avoid expanding scope beyond the repair path itself.",
    "RESOLVED (was a second, more severe finding from the same independent review): post-merge-lane-close.yml's 'Bind proof artifacts to merge SHA' step ran unconditionally on both push and workflow_dispatch, passing github.sha as --merge-sha. On workflow_dispatch (the only way to run the trusted --repair-merged replay for UTV2-1585/UTV2-1586) github.sha is main's current tip, not the historical lane's authoritative merge SHA. Because model-routing.json's closeout_binding is intentionally immutable-once-bound (unlike evidence.json/verification.md's last-write-wins rebind), this step would have bound model-routing.json to the wrong SHA on any workflow_dispatch replay attempt, and the immediately-following --repair-merged step's attempt to bind the real SHA would then fail closed with binding_conflict -- permanently deadlocking the exact replay UTV2-1585/UTV2-1586 are queued for. PM authorized a second narrow scope amendment: the step now runs on push events only (github.event_name == 'push'); --repair-merged's own rebindRepairedLaneProof (this issue's fix) already binds model-routing.json authoritatively on the trusted repair path, so the early bind is unneeded there. A regression test asserts the step's if: condition requires github.event_name == 'push'.",
    "ACCEPTED, not fixed: a second fresh-context independent review of the workflow fix noted that the now-skipped-on-workflow_dispatch early bind step used to run the full ops:proof-generate CLI (generateProofArtifacts), which creates diff-summary.md/verification.md from scratch when missing -- unlike rebindRepairedLaneProof()'s rebindMergeSha(), which only rebinds evidence.json/verification.md if they already exist and otherwise reports 'missing' without creating them. A lane that reached a workflow_dispatch --repair-merged replay having never had proof-generate run against it at all (not merely missing SHA binding) would now stay without those files. Not a live issue: both actual replay candidates, UTV2-1585 and UTV2-1586, already have populated evidence.json (with an existing sha_binding object) and verification.md in the repository today, so rebindMergeSha correctly updates them. Left undocumented-until-now theoretical gap for a future lane that skipped normal proof generation entirely before merge -- an already-abnormal state, not fixed here to avoid expanding scope beyond the repair path and the one workflow condition.",
    "RESOLVED: an automated Codex review found rebindModelRoutingJsonSha accepted any required sidecar with a matching issue_id regardless of whether it carried real execution-provenance fields -- a truncated/tampered sidecar such as {\"issue_id\":\"UTV2-1586\"} would receive an authoritative closeout_binding despite providing no evidence of which model executed the lane. Fixed by requiring non-empty model and reasoning_effort string fields on any required sidecar, checked after identity validation and before the existing conflict check. Deliberately narrowed to these two fields (not also model_profile/policy_version, which are administrative metadata rather than execution provenance) so legitimate historical sidecars that never carried the latter two are not rejected; the real UTV2-1585 and UTV2-1586 fixtures and all existing sidecar fixtures in this test suite already carry both required fields and are unaffected.",
    "ACCEPTED, not fixed: a third fresh-context independent review of the provenance-field fix confirmed no bypass (the check is unconditional on `required` and runs before the existingBinding conflict/unchanged branch) and no drift across call sites, but noted the check only requires model/reasoning_effort to be non-empty strings, not that they match the lane manifest's own model_routing.model/model_routing.reasoning_effort fields (which use the identical key names and are already in scope at both call sites) -- so a sidecar with well-formed but fabricated/copy-pasted values from a different lane would still pass. Presence is weaker than truthfulness. Left as a follow-up: cross-checking against an optional (Codex-only) manifest field needs its own scoping to avoid new false-positive rejections for lanes where that block is absent or stale, which was judged to warrant a separate, deliberate pass rather than folding into this already-multiply-amended PR.",
    "ACCEPTED, not fixed: the same review also noted the existing UTV2-1585/UTV2-1586 fixture tests in scripts/ops/proof-generate.test.ts synthesize an equivalent payload via preMergeModelRoutingJson() rather than reading the real committed docs/06_status/proof/UTV2-1585/model-routing.json and UTV2-1586/model-routing.json files directly. The reviewer independently read both real files and confirmed they pass the new validation (and scanned all 17 model-routing.json files in the repo -- none would be rejected), so there is no live regression risk; tightening the fixture tests to read the real files directly is a test-fidelity improvement, not a defect, and was left for a follow-up pass.",
    "RESOLVED (a fifth review finding, showing the second scope amendment was incomplete): an automated Codex review found the push-only restriction on 'Bind proof artifacts to merge SHA' did not cover every case -- a governed manifest-only repair PR (buildRepairRequiredViaPrPacket's recommended path for a lane whose pr_url is already set, e.g. UTV2-1586) merges via an ordinary push, and that push's own github.sha is the repair PR's merge commit, not the lane's original implementation merge SHA, even though github.event_name == 'push' is true. The manifest checked out by that same push already carries the correct historical commit_sha (the repair PR's whole point), so the step would still bind model-routing.json to the wrong SHA and hit the identical binding_conflict deadlock later in the same run. Fixed by additionally comparing the checked-out manifest's own commit_sha against this push's SHA and skipping the early bind when they already disagree -- the signal that this push is a repair commit, not the lane's own first-time closeout."
  ],
  "sign_off": {
    "static_proof_status": "PASS",
    "runtime_proof_status": "PASS",
    "ready_for_pm_review": true
  }
}
