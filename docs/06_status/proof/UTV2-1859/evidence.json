{
  "schema_version": 2,
  "issue_id": "UTV2-1859",
  "tier": "T2",
  "lane_type": "delivery-ui",
  "proof_profile": "static",
  "status": "READY_FOR_REVIEW",
  "sha_binding": {
    "merge_sha": null,
    "verified_source_sha": "c10f289cbc654c2f5ba2ac152c5685b49d57a3dc",
    "evidence_commit_sha": null,
    "current_pr_head_sha": "c10f289cbc654c2f5ba2ac152c5685b49d57a3dc",
    "sha_type": "pending_merge",
    "ci_sentinels": {
      "pnpm_type_check": "PASS - exit 0, no diagnostics. Re-measured in the lane worktree at this anchor after the second main resync.",
      "pnpm_test": "PASS - the two touched suites report tests 58, pass 58, fail 0, exit 0 (pnpm exec tsx --test apps/smart-form/test/form-utils.test.ts apps/smart-form/test/api-client.test.ts), re-measured at this anchor. The full suite runs in the required `verify` check on this PR, which is the authoritative result; no local full-suite PASS is claimed.",
      "playwright_e2e": "PASS - npx playwright test -c apps/smart-form/playwright.config.ts apps/smart-form/e2e/smart-form-submission.spec.ts --workers=1: 14 passed (1.1m), exit 0, re-run at this anchor. This is the run that demonstrates the browser now issues POST /api/submissions for a canonical player prop with no scheduled event.",
      "r_level_check": "PASS - npx tsx scripts/ci/r-level-check.ts --issue UTV2-1859 at this anchor: Verdict PASS, Changed files 9, Rules matched operator-ui. This supersedes the pre-resync reading (Changed files 3, Rules matched none): the check compares against main, and the two resync merges pulled UTV2-1843 documentation into the comparison. PASS in both readings; the earlier numbers are superseded rather than left standing. The count still does not equal the four files this lane changed, and that is reported rather than reconciled.",
      "pnpm_lint": "PASS - exit 0, no findings, re-measured at this anchor. Added after the first CI run failed on it: the initial attestation ran pnpm type-check and the touched suites but never pnpm lint, which verify:static runs, so it claimed a green tree that had not been fully measured. See the lint_regression entry in static_proof."
    },
    "anchor_note": "c10f289cbc654c2f5ba2ac152c5685b49d57a3dc is the second main-resync merge commit and the last commit on this branch changing any file outside PROOF_ONLY_PREFIXES. Every command recorded here was re-run against that tree rather than carried forward. It replaced 2902c3e92, which replaced 1cbdbc88d; the first replacement was forced by a CI lint failure that changed the code, and the two after it by main resyncs required because branch protection is strict. Both resyncs brought documentation only and zero application code, which is why every count except the r-level file count is unchanged -- and the r-level count moved because that check compares against main, not because this lane changed.\\n\\nRule 4 of scripts/ci/proof-binding-validator.ts is a two-dot `git diff verified_source_sha..HEAD`, so every commit after this one must stay inside docs/06_status/proof/ or docs/06_status/lanes/. The only commit after it is the bundle re-anchor, which does.",
    "bound_at": "2026-09-08T15:05:00.000Z"
  },
  "static_proof": {
    "status": "PASS",
    "type_check": {
      "status": "PASS",
      "command": "pnpm type-check",
      "detail": "exit 0, no diagnostics"
    },
    "test": {
      "status": "PASS",
      "command": "pnpm test",
      "tests": 58,
      "failures": 0,
      "detail": "the two touched suites at the verified source SHA: tests 58, pass 58, fail 0, exit 0"
    },
    "no_new_gate": {
      "status": "PASS",
      "command": "git diff origin/main --name-only -- .github/",
      "observed": "empty",
      "significance": "No workflow, check, label, approval artifact or lane type is added. verify, Executor Result Validation, Merge Gate and P0 Protocol are unchanged."
    },
    "scope": {
      "status": "PASS",
      "detail": "Four files, all under apps/smart-form/, matching file_scope_lock exactly. No server file and no contract is touched."
    },
    "server_rule_citations_verified": {
      "status": "PASS",
      "detail": "All three // SERVER-RULE: fragments cited by apps/smart-form/lib/form-utils.ts were confirmed present in origin/main's apps/api/src/smart-form-validation.ts, so the coupling test is not vacuous and this lane is green on a main base independently of the open server PR."
    },
    "lint": {
      "status": "PASS",
      "command": "pnpm lint",
      "detail": "exit 0, no findings"
    },
    "lint_regression_corrected": {
      "status": "PASS",
      "detail": "The first CI run on this branch failed verify at apps/smart-form/e2e/smart-form-submission.spec.ts:1469:65 with '@typescript-eslint/no-explicit-any: Unexpected any'. The payload assertion used Record<string, any>; it is now an explicit shape naming distributionMode, eventId and the participantResolution fields the test actually reads, which is a better assertion target than `any` independently of the rule. Recorded rather than quietly amended: the local gate for this lane was type-check plus the touched suites, and lint was never re-run after the code was written -- preflight's verify:quick ran at lane-start, before the code existed.",
      "reverification": "pnpm lint exit 0, pnpm type-check exit 0, and the browser suite re-run after the retype: 14 passed (1.1m), exit 0. The retype changes the exact object the Track Only and provenance assertions destructure, so the suite was re-run rather than assumed unaffected."
    }
  },
  "runtime_proof": {
    "status": "PASS",
    "kind": "browser",
    "detail": "A Playwright run drives the form's own code in a real browser and asserts exactly one POST /api/submissions is issued for a canonical player prop with no scheduled event, then asserts on the captured payload that metadata.distributionMode is track-only, that participant resolution is canonical, that the event id is null on both the resolution and the metadata, and that the resolved player carries its teamId. The submission endpoint and all reference data are intercepted, so no live server is contacted and no credential is placed in the Playwright environment. Re-run after the payload retype and again at the verified source SHA: 14 passed (1.1m), exit 0, both times.",
    "mutations": [
      {
        "name": "stale SERVER-RULE citation",
        "expected": "the coupling control fails",
        "observed": "tests 33, pass 32, fail 1 - \"client guard cites a server rule that is no longer in apps/api/src/smart-form-validation.ts: manual participant resolution requires at least one entered PARTICIPANTX\"",
        "significance": "This is the control that would have caught the server-side rule removal that created this defect."
      },
      {
        "name": "restore the deleted client guard (unit)",
        "expected": "the two behaviour tests fail",
        "observed": "tests 58, pass 55, fail 3 - including 'a canonical player prop is admitted without a canonical event' and 'no client guard refuses a structured-fallback player prop that has no event'. Restoring the file returns 58/58."
      },
      {
        "name": "restore the deleted client guard (browser)",
        "expected": "the browser test fails because no request is issued",
        "observed": "1 failed at e2e/smart-form-submission.spec.ts:1366 - expect.poll(() => submissionRequests).toBe(1) reported Expected: 1, Received: 0, Timeout 5000ms exceeded while waiting on the predicate.",
        "significance": "This is the originally reported symptom reproduced exactly: the form refuses before issuing a request. Server-side evidence cannot observe this, because the request is never sent."
      }
    ]
  },
  "known_gaps": [
    "Does not claim the deployed Smart Form works. Production is d3f69b804 and neither this change nor the server-side participants repair is running there. Dispatching a production deployment is a reserved decision.",
    "Does not claim Milestone 1 step 4 is performed. That step is an operator action by Griff against the deployed system; this is a browser run against locally built code with the submission endpoint intercepted.",
    "Does not claim a database write. Persistence is asserted by pnpm test:db and the Writable DB proof (staging only) job, not by this bundle.",
    "This client change and the open server-side participants PR must reach production together. Shipping this alone would let the browser send a request the deployed server still refuses."
  ]
}
