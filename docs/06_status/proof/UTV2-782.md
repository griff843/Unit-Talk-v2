# UTV2-782 Proof: WAL/PITR Restore Runbook + Backup Alert Wiring

**Merge SHA:** f91b70dbc3c254fb2d06718657fd8f948f1f834c
**PR:** https://github.com/griff843/Unit-Talk-v2/pull/514
**Branch:** claude/utv2-782-walpitr-restore-proof
**Date:** 2026-04-30

---

## Acceptance Criteria Status

| AC | Description | Status | Evidence |
|----|-------------|--------|----------|
| AC1 | Hetzner WAL archiving enabled (archive_mode=on, archive_timeout=300) | Pending Hetzner execution | Runbook step 1 |
| AC2 | Daily basebackup cron on Storage Box | Pending Hetzner execution | Runbook step 2 |
| AC3 | Second-provider backup (B2 or second Storage Box) | Pending Hetzner execution | Runbook step 3 |
| AC4 | backup-alert-check.ts deployed as monitoring cron | Pending Hetzner execution | Runbook step 4 |
| AC5 | Isolated restore test performed (recovery.signal + WAL replay) | Pending Hetzner execution | Runbook step 5 |
| AC6 | Table verification queries pass post-restore | Pending Hetzner execution | Runbook step 6 |
| AC7 | backup-alert-check.ts alerts correctly on missing heartbeat | DELIVERED (code) | Live output below |

---

## Code Deliverables (Shipped)

### AC7: backup-alert-check.ts — Live DB Output

Script run against live Supabase (2026-04-30T23:48:23.100Z):

```json
{
  "level": "CRITICAL",
  "service": "backup",
  "check": "daily",
  "ageHours": null,
  "message": "No backup.daily rows found in system_runs — backup has never run or heartbeat was never wired.",
  "ts": "2026-04-30T23:48:23.100Z"
}
```

**Interpretation:** CRITICAL with `ageHours: null` is the correct pre-backup state. No `backup.daily` row exists in `system_runs` because Hetzner backup cron has not yet been configured. The script correctly surfaces this as a monitoring alert. Once Hetzner cron writes its first heartbeat, the alert will clear (OK) and will re-alert if the row goes stale.

### Unit Tests: 7/7 Pass

```
✔ parseBackupThresholdHours rejects zero, negative, and non-numeric values
✔ parseBackupThresholdHours accepts valid positive numbers including decimals
✔ evaluateBackupFinding returns OK for a backup within threshold
✔ evaluateBackupFinding returns CRITICAL when backup exceeds threshold
✔ evaluateBackupFinding returns CRITICAL when no backup record exists
✔ evaluateBackupFinding treats age exactly equal to threshold as OK
✔ evaluateBackupFinding rounds age to one decimal place

tests 7 | pass 7 | fail 0
```

---

## Runtime Proof (Pending Hetzner)

To be filled by operator after completing runbook steps 1–7:

```
WAL archiving enabled: [ ] date:
Basebackup cron installed: [ ] schedule:
Second-provider backup: [ ] method: (B2 / second Storage Box)
Alert cron deployed: [ ] host:
Restore test completed: [ ] date:
Post-restore verification: [ ] tables checked:
RPO achieved: [ ] hours
RTO achieved: [ ] minutes
```

---

## Shipped Artifacts

- `scripts/backup-alert-check.ts` — alert script with pure evaluator functions
- `scripts/backup-alert-check.test.ts` — 7-test unit coverage
- `docs/05_operations/WALPITR_RESTORE_RUNBOOK.md` — 7-step ops runbook
