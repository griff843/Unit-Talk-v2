# Repo Bootstrap

## Current Repo Decisions

- Monorepo package manager: `pnpm`
- Language baseline: `TypeScript`
- Module system: `NodeNext`
- Apps and shared packages are first-class workspace members

## Immediate Next Repo Tasks

- Add test runner and baseline smoke tests
- Add branch protections
- Add environment loading and runtime validation in shared config
- Expand schema migration and type generation workflow
