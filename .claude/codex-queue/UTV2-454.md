# Codex Task Packet — UTV2-454

Work only this Linear issue.

You are not exploring the repo. You are executing a bounded task packet.

Required output:
1. implement only the scoped issue
2. touch only allowed files
3. do not modify forbidden files
4. run the required verification commands
5. summarize what changed
6. provide PR-ready summary
7. stop if scope is ambiguous or collides with active work

---

## Task packet

* **Linear issue:** UTV2-454 — Update PROGRAM_STATUS.md for Wave 2 data layer closure
* **Why it matters:** PROGRAM_STATUS.md has not been updated since the CC-M5 batch on 2026-04-08. Wave 2 data layer (UTV2-448 through UTV2-453, all merged 2026-04-08) is not reflected. Test counts, capability state, and current milestone description are stale.

* **Allowed files:**
  * `docs/06_status/PROGRAM_STATUS.md`

* **Forbidden files:** all others

---

## Acceptance criteria

- [ ] New "Last Updated" entry added at the top of the `## Last Updated` section:
  ```
  2026-04-08 — Wave 2 data layer complete (UTV2-448–453, merged 2026-04-08).
  ```
  Entry must list all 6 issues with their PR numbers and what they accomplished:
  - UTV2-448 (PR #198): results-resolver rewrite — use `odds.<oddID>.score` per SGO support
  - UTV2-449 (PR #199): CLV opening line fallback — removes Odds API dependency from CLV critical path
  - UTV2-450 (PR #200): market alias expansion — NHL, NFL, NCAAF, MLB player props added to `SGO_MARKET_KEY_TO_CANONICAL_ID`; all sport spread/ML/total game-line aliases added to `SGO_GAME_LINE_CANONICAL_ID`
  - UTV2-451 (PR #201): `is_closing` auto-marking — `markClosingLines()` confirmed already implemented in `ingest-league.ts` (closed as already done)
  - UTV2-452 (PR #202): grading retry queue — picks with `game_result_not_found` re-queued up to 3 attempts at 15-minute backoff
  - UTV2-453 (PR #202): CLV pre-resolved context — `computeAndAttachCLV` now receives grading's already-resolved event via `CLVPreResolvedContext`, eliminating wrong-event selection that produced null CLV

  **Note:** UTV2-451 was verified already done (no code change). UTV2-452 and UTV2-453 share PR #202 (squash-merged together into main).

- [ ] **Test counts** updated in `## Current State` table (Tests row):
  - Root `pnpm test`: **188 pass** (unchanged)
  - Ingestor standalone: **55 pass** (was 51)
  - Grading service standalone: **31 pass** (up from ~27)
  - Grading cron standalone: **3 pass** (new)

- [ ] **Milestone** field in Current State table updated to:
  ```
  Wave 2 data layer complete (2026-04-08). Grading accuracy, CLV fallback, market alias coverage, and grading retry all shipped. G12 canary active — 7-day/≥30 graded pick burn-in required before Phase 7. Phase 7 awaiting PM approval.
  ```

- [ ] **Roadmap** entry in Current State table updated to include:
  ```
  Wave 2 data layer Done: UTV2-448 (PR #198), UTV2-449 (PR #199), UTV2-450 (PR #200), UTV2-452 (PR #202), UTV2-453 (PR #202). G12 canary active. No Ready issues. Phase 7 (Syndicate Lane, PM approval required) deferred.
  ```

- [ ] No invented claims — every statement must reflect merged/verified reality as described in this packet
- [ ] No changes to any other section (Gate Notes, Live Routing, Honest Assessment, Runner Architecture, etc.)

---

## Verification

```bash
# Check the file can be read without error (no broken markdown):
node -e "require('fs').readFileSync('docs/06_status/PROGRAM_STATUS.md', 'utf8')" && echo OK
```

No code test commands needed — this is a docs-only update.

---

## Merge dependencies

* None — docs only, no code changes

## Rollback note

* Revert the single file. No migrations, no code.

---

## Rules

* no opportunistic refactors
* no unrelated cleanup
* no scope expansion
* if blocked, stop and report the precise blocker
